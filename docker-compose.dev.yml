# Surcharge développement local — expose Redis et l'app sur toutes les interfaces.
# Usage : docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d

services:
  redis:
    ports:
      - "127.0.0.1:6379:6379"

  web:
    ports:
      - "${PORT:-7860}:7860"
